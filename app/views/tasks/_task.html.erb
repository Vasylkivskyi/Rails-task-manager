<li>
  <%= form_with(model: task, class: "text-lg flex items-center my-3 w-72") do |f| %>
    <%= f.check_box :complited, class: "w-4 mr-1 h-4 text-green-600 bg-gray-100 border-gray-300 rounded checked:bg-green-500" %>
    <%= task.description %>
  <% end %>
</li>
